const frutas = new Array('banana', 'manzana', 'pera', 'fresa', 'uva', 'melon', 'piña', 'naranja'); // el length es 8

for(let i = 0; i < frutas.length; i++) {
    console.log(`La fruta en el indice ${i} es ${frutas[i]}`);
}
